//МАССИВЫ

// length — длина массива  

// concat — слияние массивов вместе  

// join — слияние массива в строку  

// reverse — переворот массива  

// includes — проверка наличия в массиве  

// indexOf — поиск по массиву  

// lastIndexOf — поиск по массиву с конца  

// slice — отрезание части массива  

// splice — удаление/вставка элементов в массив  

// shift — удаление первого элемента  

// pop — удаление последнего элемента  

// unshift — добавление элементов в начало массива  

// push — добавление элементов в конец массива  

// map — перебор массива и создание нового массива  

// forEach — перебор массива без создания нового массива  

// filter — фильтрация массива по условию  

// every — проверка, удовлетворяют ли все элементы условию  

// some — проверка, удовлетворяет ли хотя бы один элемент условию  

// reduce — сведение массива к одному значению  

// reduceRight — сведение массива к одному значению с конца  

// sort — сортировка массива  

// fill — заполнение массива заданным значением  

// find — поиск первого элемента, удовлетворяющего условию  

// findIndex — поиск индекса первого элемента, удовлетворяющего условию  

// findLast — поиск последнего элемента, удовлетворяющего условию  

// findLastIndex — поиск индекса последнего элемента, удовлетворяющего условию  

// flat — получение плоского массива из подмассивов  

// flatMap — получение плоского массива из подмассивов с преобразованием  

// Array.isArray — проверка, является ли объект массивом  

// Array.from — получение массива из массивоподобного объекта  

// Array.of — получение массива из параметров  

// at — получение элемента массива по позиции  
